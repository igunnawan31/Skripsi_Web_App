export type User = {
    id: string;
    name: string;
    email: string;
    majorRole: string;
    minorRole: string;
}

export type UserResponse = {
    id: string;
    name: string;
    email: string;
    majorRole: string;
    minorRole: string;
    photo: string;
}